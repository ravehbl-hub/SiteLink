{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "corepack pnpm install --no-frozen-lockfile && corepack pnpm --filter @sitelink/shared build && corepack pnpm --filter @sitelink/backend exec prisma generate"
  },
  "deploy": {
    "startCommand": "corepack pnpm --filter @sitelink/backend exec tsx src/server.ts",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE"
  }
}
